package com.cousersoft.game;

/**
 * Type-safe enum representing all possible game states.
 * Replaces the previous String-based state system for compile-time safety.
 */
public enum GameState {
    MENU,
    HELP,
    GAME,
    SHOP,
    GAMEOVER
}
